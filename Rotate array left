 for(int i = 0 ; i < d ; i++){
        
            int j, temp; 
        temp = arr[0]; 
        for (j = 0; j < arr.length - 1; j++) 
            arr[j] = arr[j + 1]; 
        arr[j] = temp;
            
            
        }
