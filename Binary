int first = 0 , last = arr.length - 1;
        
          while(first <= last){
            
            int mid = (first + last)/2;
            
            if(arr[mid] == num)
                return mid;
            
           else if(arr[mid] < num)
                first = mid + 1;
                
            else
                 last = mid - 1;
        
    }
  return -1 ;       
